import { ConsultationBlockProps } from "./ConsultationBlock.types"
import styles from "./ConsultationBlock.module.css"
export function Page({ keyPoints, title }: ConsultationBlockProps) {
	return <div></div>
}
